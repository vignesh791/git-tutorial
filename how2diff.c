#include<stdio.h>

int main()
{
    printf("%s\n", "How git diff works?");
    printf("%s\n", "Does a diff btwn latest ver and staged copy");
    printf("%s\n", "If a committed file was modified, does a diff "
                    "btwn latest ver and committed file");
    printf("%s\n", "git diff --cached does a diff btwn staged copy "
                    "and committed copy");

    return 0;
}    
